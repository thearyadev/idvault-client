import { StyleSheet } from "react-native";

export const inputStyle = StyleSheet.create({
  input: {
    height: 50,
    width: "100%",
    borderRadius: 6,
    borderWidth: 0.5,
    padding: 15,
    color: "black",
    borderColor: "gray",
  },
});
